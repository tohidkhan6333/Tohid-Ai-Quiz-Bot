# render.yaml (अगर use करें)
services:
  - type: web
    name: tohid-quiz-bot
    env: node
    buildCommand: npm install --production
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: MONGODB_URI
        sync: false